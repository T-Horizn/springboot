spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC
    username: root
    password: root
#设置Mp相关的配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix: t_
